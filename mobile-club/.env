# Configuration pour le développement local
# Le backend FIELDZ doit tourner sur le port 8080

# Pour iOS Simulator et Expo web:
EXPO_PUBLIC_API_URL=http://localhost:8080

# Pour Android Emulator, décommentez cette ligne et commentez celle du dessus:
# EXPO_PUBLIC_API_URL=http://10.0.2.2:8080

# Pour tester sur un appareil physique, utilisez l'IP locale de votre Mac:
# 1. Trouvez votre IP: ifconfig | grep "inet " | grep -v 127.0.0.1
# 2. Utilisez: EXPO_PUBLIC_API_URL=http://[VOTRE_IP]:8080
# 3. Ajoutez cette IP dans backend/src/main/resources/application-dev.yml (cors.allowed-origins)
